import React from 'react';

const Content = ({username}) => {
  return (
    <div>
      wow, it sure would be nice to have some app content here for {username}
    </div>
  );
};

export default Content;
